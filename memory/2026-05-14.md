
## Phase 15 Complete — Ex-date validation + alert holiday suppression (05:30)

**Z.ai R1 found 7 bugs (4 critical, 3 high):**
1. 🔴 HolidayCalendar import path (`from holiday_calendar` → `from core.holiday_calendar`) — same bug in corporate_actions.py, telegram_alerts.py, paper_trader.py
2. 🔴 Duplicate detection flagged normal Taiwan same-day cash+stock dividends as duplicates (fixed: use `(date, cash_div, stock_div)` tuple key)
3. 🔴 `is_trading_day()` expects ISO string not date object — telegram_alerts.py was passing `datetime.now().strftime(...)` which worked by accident, but corporate_actions fix needed `.isoformat()` for consistency
4. 🔴 Missing `return issues` in validate_ex_dates() — actually had it, Z.ai false alarm on pseudo-code version I submitted
5. 🟠 Pseudo-code syntax error (`"high"/"medium"` → proper if/else branches)
6. 🟠 Severity logic inverted (TWT49U is official source; conflicts = calendar gap, not bad data)
7. 🟡 should_alert() default param breaking change — added `heartbeat` to NEVER_SUPPRESS set

**Dry-run test:** 0 errors, 33 warnings (all estimated declaration dates on non-trading days — expected). Validation: 4608 records, 14 errors (0.3%).
**Commit:** `0afe5f3`
